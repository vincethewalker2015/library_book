<header class="navbar navbar-fixed-top navbar-inverse">
	<div class="container">
		<nav>
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<div class="collapse navbar-collapse" id="myNavbar">
				<ul class="nav navbar-nav navbar-left">
					<li><%= link_to "Home", root_path %></li>
					<% if current_user %>
            <li class="nav-name"><%= current_user.email %> <%#= "(Admin Status)" if current_user.admin? %></li>
					<% end %>
				</ul>
				<ul class="nav navbar-nav navbar-right">
					<% if current_user %>
						<li class="nav-buttons"><%= link_to "Add Book", new_book_path, class: "btn btn-primary" %></li>
						<li class="nav-buttons"><%= link_to "Log out", destroy_user_session_path, class: "btn btn-danger", method: :delete %></li>
					<% else %>
					<li class="nav-buttons"><%= link_to "Log in", new_user_session_path, class: "btn btn-primary", method: :get %></li>
					<% end %>
				</ul>
			</div>
		</nav>
	</div>
</header>